import React from "react";
import ReactDOM from "react-dom";
import App from "./components/App";

ReactDOM.render(<App />, document.getElementById("root"));

/* 
Using Hook to change the state of the Page meaning Rendering again, 
cause without using Hook the Page state will not change.
Hook can be used to make the Page more interactive. 
*/
